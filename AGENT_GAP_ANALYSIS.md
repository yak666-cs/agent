# 当前项目离“真正 Agent”还缺什么

这份文档是对当前项目的一次工程视角梳理。

目标不是说“现在这套不能用”，而是说明：

- 它已经具备哪些 Agent 雏形
- 它为什么还不像一个成熟的 Agent 系统
- 如果想继续往 Claude Code / LangGraph / OpenHands 这类更强的方向靠，还缺哪些关键模块

为了更容易理解，文档里会结合 `Claude Code` 的典型使用场景来解释。

---

## 一句话结论

当前项目更像：

`带工具调用能力的 LLM 循环`

而不是：

`能稳定完成复杂任务、可恢复、可验证、可观测的 Agent 系统`

换句话说，现在已经有 Agent 的“骨架”，但还缺少真正让它稳定工作的“执行框架”。

---

## 当前已经有的部分

从代码结构看，项目已经具备这些基础能力：

- `agent/loop.py`
  负责主循环、LLM 调用、工具执行、重试、熔断、状态回调
- `agent/subagent.py`
  负责子任务拆解，以及串行 / 并行 / 依赖执行
- `agent/context.py`
  负责上下文拼接、裁剪
- `tools/`
  负责工具注册、工具执行、沙箱、缓存
- `harness/observability.py`
  负责 trace、metrics、观测事件
- `harness/resilience.py`
  负责 retry / circuit breaker
- `tests_plan/`
  已经开始覆盖 observability、memory、sandbox、subagent 等能力

这说明你的方向是对的，底层并不是从零开始。

问题主要不在“有没有模块”，而在“这些模块还没有被收敛成一个稳定的 Agent Runtime”。

---

## 为什么说它还不是“真正的 Agent”

可以先拿 `Claude Code` 做类比。

你在 Claude Code 里输入：

`帮我分析这个仓库为什么测试慢，先找最慢的测试，再看是不是数据库初始化有问题，最后给优化建议并改掉最明显的一处。`

一个成熟 Agent 在背后通常会做这些事：

1. 先识别这是一个复杂任务，不是普通问答
2. 自动拆成多个阶段
3. 每个阶段使用不同工具
4. 中途记录当前进度
5. 某一步失败后只重试那一步
6. 最后检查自己是不是真的完成了全部要求

而你当前项目更像是：

1. 把用户问题直接丢给 LLM
2. 模型自己临场决定要不要拆任务
3. 工具能调，但没有足够强的流程约束
4. 中途失败时，容易整体中断或状态混乱
5. 最终能回答，但不一定真的“稳定完成任务”

所以差距不在“会不会调用工具”，而在“是否具备稳定的任务执行系统”。

---

## 还缺哪些关键模块

下面按“真正 Agent 常见能力”来拆。

---

## 1. 缺少更强的任务编排器

### 现在是什么

当前主流程还是 `LLM -> 工具 -> LLM -> 工具 -> 最终回答`。

虽然你已经有 `subagent.py`，但是否拆任务、怎么拆、什么时候进入复杂流程，仍然主要靠模型自己判断。

### 问题是什么

这种方式在简单任务上很好用，但一旦任务复杂，就会出现这些问题：

- 有时会跳步骤
- 有时会太早下结论
- 有时中间失败后不知道怎么续
- 有时明明应该并行做，结果串行做了很久

### Claude Code 场景类比

比如用户说：

`帮我定位这个前端页面卡顿的原因，顺便把最主要的问题修掉。`

成熟 Agent 会自动进入类似这样的流程：

- 阶段 1：读取项目结构
- 阶段 2：识别渲染热点
- 阶段 3：运行测试或构建
- 阶段 4：定位瓶颈
- 阶段 5：修改代码
- 阶段 6：验证结果

而不是让模型自由发挥。

### 你还缺什么

你还缺一个更明确的 `planner/orchestrator`，把复杂任务固定成工作流，而不是只靠模型即兴发挥。

---

## 2. 状态机还不完整

### 现在是什么

你已经有状态字段和状态回调，比如：

- `thinking`
- `executing`
- `done`
- `error`

### 问题是什么

这些状态更像“运行标签”，还不是“任务状态机”。

真正 Agent 的状态通常更细，因为状态不仅要拿来显示，还要控制流程。

### Claude Code 场景类比

比如用户说：

`先重构，再补测试，再跑测试，如果失败就继续修。`

那么系统至少要区分：

- `planning`
- `editing`
- `testing`
- `fixing_failed_tests`
- `verifying`
- `completed`

否则前端看起来只是“执行中”，但实际上系统自己也不知道当前具体在哪一步。

### 你还缺什么

你还缺一个更完整的任务状态机，用来描述阶段、推进逻辑、失败恢复和 UI 展示。

---

## 3. 缺少任务状态持久化

### 现在是什么

目前已经有 observability 和部分运行态记录，但核心任务状态还不够像独立的任务存储。

### 问题是什么

复杂 Agent 不是一次性回答器，它更像一个“持续执行器”。

如果中途：

- 进程重启
- 前端断开
- 子任务失败
- 用户切换会话

系统应该知道自己执行到了哪一步，而不是重新从头猜。

### Claude Code 场景类比

比如让 Claude Code：

`遍历这个单仓库，把所有过时的 API 调用迁移掉，然后生成迁移报告。`

这类任务可能要跑很久。

成熟系统会保存：

- 已完成哪些目录
- 哪些文件改过
- 哪些地方失败了
- 当前还剩哪些待执行项

这样断了也能继续。

### 你还缺什么

你还缺一个更明确的 `task store / run store / checkpoint store`，保存：

- 当前计划
- 子任务状态
- 每步输入输出
- 工具证据
- 错误原因
- 恢复点

---

## 4. 缺少 verifier / critic / evaluator

### 现在是什么

系统现在更偏“能做事”，但“会不会验收”还不够强。

### 问题是什么

Agent 最大的问题之一不是不会做，而是“以为自己做完了”。

### Claude Code 场景类比

用户说：

`帮我修这个 bug，并确保不影响其他功能。`

一个成熟 Agent 不该只改完代码就停，而应该继续检查：

- 测试有没有跑
- 相关功能有没有回归
- 用户要求的条件是不是都满足
- 修复是不是只改表面，根因还在不在

### 你还缺什么

你还缺一层专门的 `verifier` 或 `critic`，负责：

- 检查任务是否真正完成
- 检查证据是否足够
- 检查是否漏项
- 检查结果是否自相矛盾
- 决定要不要补跑一步

这层非常关键，因为它直接决定 Agent 是“像人一样交付”，还是“像模型一样回答”。

---

## 5. 记忆系统还不够工程化

### 现在是什么

项目里已经有 memory 相关内容，也有测试，但还没有成为整个执行链路的核心部分。

### 问题是什么

真正 Agent 的记忆不只是“保存几条历史消息”。

它通常至少分成几层：

- 短期会话记忆
- 用户偏好记忆
- 任务过程记忆
- 工具结果记忆
- 可检索长期记忆

### Claude Code 场景类比

假设你反复让 Claude Code 帮你改项目：

- 你习惯 `apply_patch`
- 你不喜欢改无关文件
- 你偏好中文说明
- 你项目里某些目录不要碰

成熟 Agent 应该在后续任务里自动记住这些偏好，而不是每次重新理解。

### 你还缺什么

你现在还缺“分层记忆策略”和“记忆参与执行决策”的机制，而不只是有 memory 模块。

---

## 6. 工具层还缺语义约束

### 现在是什么

你已经有：

- 工具注册
- 工具执行器
- 沙箱
- 缓存

### 问题是什么

这说明工具“能跑”，但还不等于工具“适合复杂任务编排”。

真正 Agent 的工具层不仅是函数集合，还会描述：

- 什么时候适合调用
- 什么时候不该调用
- 输入格式约束
- 输出结构约束
- 失败类型
- 是否有副作用

### Claude Code 场景类比

比如 `bash` 工具。

成熟 Agent 不只知道“我可以跑 bash”，还应该知道：

- 这是危险工具
- 改文件前最好先读
- 删文件前需要确认
- 某些命令可以自动重试
- 某些命令失败后要改走别的路线

### 你还缺什么

你还缺更强的工具语义层，比如：

- tool schema 更标准化
- tool capability 描述
- tool failure taxonomy
- tool side-effect 标记
- 工具组合模板

---

## 7. 缺少 human-in-the-loop 节点

### 现在是什么

现在有沙箱和一定的安全控制，但“人工决策点”还不够明确。

### 问题是什么

成熟 Agent 不是所有事情都自动执行。

有些操作天然应该暂停，让用户确认。

### Claude Code 场景类比

比如下面这些情况：

- 批量删文件
- 改环境变量
- 跑高代价命令
- 覆盖大量已有代码
- 改数据库 schema

Claude Code 这类系统之所以让人敢用，不只是因为它能做，而是因为它知道什么时候该停下来问你。

### 你还缺什么

你还缺更明确的审批节点机制，比如：

- 风险分级
- 自动暂停点
- 用户确认后继续执行
- 拒绝后自动换方案

---

## 8. 失败恢复机制还不够完整

### 现在是什么

你已经有 retry 和 circuit breaker。

### 问题是什么

这只能解决“调用失败”这一类问题，不能解决“任务级失败恢复”。

真正复杂任务里，失败常常是这样的：

- 第 2 步成功，第 3 步失败
- 某个子任务超时
- 某个工具结果不完整
- 某个目录处理失败，但其他目录已经成功

这时系统不应该全盘重来。

### Claude Code 场景类比

比如：

`把 tests 目录下所有过时 mock 改掉，并跑一遍相关测试。`

如果第 8 个文件改失败了，成熟 Agent 应该：

- 保留前 7 个成功结果
- 只重试失败部分
- 最后产出“成功 + 失败列表”

### 你还缺什么

你还缺：

- 子任务级重试
- checkpoint 恢复
- partial success 处理
- 失败补偿策略
- 可继续执行的 run resume

---

## 9. 评测体系还不够像 Agent Benchmark

### 现在是什么

你已经有不少测试，这很好。

### 问题是什么

现在的测试更偏模块级，而 Agent 最大的问题通常出在“跨模块协作”。

### Claude Code 场景类比

一个真正需要验证的问题不是：

`tool executor 能不能返回结果`

而是：

`当用户提出一个多阶段任务时，系统是否能稳定拆解、执行、失败恢复、验证并交付`

### 你还缺什么

你还缺几类更像真实 Agent 的测试：

- 复杂任务端到端测试
- 中断恢复测试
- 工具失败注入测试
- 长任务稳定性测试
- 多会话并发测试
- 结果质量评估测试

没有这些，很难知道系统是真的可靠，还是只是“演示时能跑通”。

---

## 10. 前端还不是完整的 Agent 控制台

### 现在是什么

你已经开始做 observability，这一步非常重要。

### 问题是什么

但成熟 Agent 前端不只是“聊天界面 + trace”，而更像“任务控制台”。

### Claude Code 场景类比

当 Claude Code 在做复杂任务时，用户真正关心的是：

- 当前在做哪一步
- 为什么卡住了
- 哪个工具失败了
- 是否还能继续
- 能不能手动重试某一步

### 你还缺什么

前端后续可以继续往这些方向补：

- 节点图 / DAG 图
- 子任务依赖关系
- 当前阶段高亮
- 可点击的失败节点
- 单步重试按钮
- 人工接管入口
- 任务恢复入口

---

## 最核心的 4 个缺口

如果只提炼成最重要的 4 层，其实就是：

### 1. Planner

让系统稳定决定：

- 这是不是复杂任务
- 应该怎么拆
- 先做什么后做什么

### 2. Workflow Runtime

让系统稳定执行：

- 子任务调度
- 状态流转
- 失败恢复
- 并行 / 串行控制

### 3. State Store

让系统稳定记住：

- 当前做到哪
- 哪些已经成功
- 哪些失败了
- 怎样从中断处恢复

### 4. Verifier

让系统稳定确认：

- 是否真的完成
- 是否有证据
- 是否遗漏要求
- 是否需要补一步

这 4 层补齐之后，你的系统才会从“能跑的 Agent Demo”真正进化成“可持续使用的 Agent 系统”。

---

## 适合你的优先级建议

如果你接下来要继续演进，建议按这个顺序做：

1. 先补强制规划与编排层
2. 再补任务状态持久化和 checkpoint
3. 然后补 verifier / critic
4. 再补工具语义层和失败恢复
5. 最后补完整的 Agent 控制台和端到端评测

这样收益最高，也最接近真正可用的 Agent 系统。

---

## 用最容易理解的话总结

你现在的项目已经有了：

- 会思考
- 会调工具
- 会分子任务
- 会记录一点状态

但它还不够像一个真正成熟的 Agent，因为它还不够：

- 会规划
- 会验收
- 会恢复
- 会持续执行

所以差的不是“模型更聪明一点”，而是“执行系统更完整一点”。

---

## 最后一句判断

如果把当前项目比作人：

- 现在像一个“会用很多工具的聪明实习生”
- 成熟 Agent 更像一个“会拆活、会推进、会复盘、会交付的工程负责人”

你现在最该补的，不是再多塞几个工具，而是把它从“会做事”变成“会把事情做完”。
