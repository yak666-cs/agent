# Agent Loop Lab · 闯关练习

## 如何用

每个关卡进入 `学习模式` 去试：
```powershell
python main.py --simple
```

标注 `[思考题]` 的只要你**看完代码回答**，不用跑。

---

## 🟢 第一关：基础调用（4 题）

**目标：熟悉 Agent 调工具的方式**

```
1. 看看我的电脑有几个磁盘分区

2. 帮我读一下 skills/builtin/bash.py 这个文件

3. 帮我算 500 的阶乘有多少位数字（提示：用 python_repl）

4. 帮我在桌面上创建一个叫 practice.txt 的文件，内容写 "Hello Agent"
```

**看完这一关你会理解：**
- Agent 怎么决定用哪个工具
- 只读命令自动放行 vs 写操作需要确认
- 工具结果如何回传给 LLM

---

## 🟡 第二关：多步推理（3 题）

**目标：理解 Agent 如何串联多步操作**

```
5. 桌面上有哪些文件？
   找到其中最大的 .txt 或 .md 文件，读一下内容

6. 我的电脑 CPU 型号是什么？内存多大？操作系统版本是什么？
   （一次问完，看 Agent 会调几次 bash）

7. 帮我完成这三件事：
   - 在桌面创建 test_dir 文件夹
   - 在里面创建一个 data.txt
   - 写入"2026-06-04: Agent Lab"
```

**看完这一关你会理解：**
- Agent 如何把大任务拆成多个工具调用
- 多轮循环是如何工作的
- `max_turns` 保护机制

---

## 🟠 第三关：异常场景（3 题）

**目标：观察 Agent 遇到错误怎么处理**

```
8. 帮我读一下 C:\不存在的文件.txt

9. 帮我执行这个命令：xxxxxx（随便编一个不存在的命令）

10. 帮我算一下这个 Python 代码：print(1/0)
```

**看完这一关你会理解：**
- `executor.py` 的错误捕获机制
- 工具失败后 LLM 会自己调整（还是继续报错）
- `consecutive_errors` 计数器什么时候触发

---

## 🔴 第四关：源码理解（思考题）

**目标：看完代码再回答，不跑程序**

```
11. [看 agent/loop.py]
    run() 方法里，什么情况下 Agent 会连续 3 次失败后终止？
    找到对应的 if 判断在哪一行

12. [看 skills/selector.py]
    filter_by_intent() 方法是做什么的？
    如果用户说"帮我搜索 Python 教程"，它会匹配到哪些工具？

13. [看 agent/context.py]
    build_messages() 里的上下文裁剪是怎么工作的？
    如果消息太多超出 token 预算，它删哪些、留哪些？

14. [看 skills/base.py]
    Permission 有哪几个等级？
    BashSkill 的 permission 是什么等级？

15. [看 skills/executor.py]
    工具执行超时了会返回什么？
    输出被截断的条件是什么？
```

---

## 🎯 第五关：动手改造（扩展题）

**目标：读懂代码后自己改**

```
16. 加一个 "获取当前时间" 的工具
    - 在 skills/builtin/ 下新建 current_time.py
    - 继承 BaseSkill，实现 execute() 返回当前时间
    - 在 main.py 里注册（加一行 registry.register()）

17. 把 wmic 加到安全白名单里
    - 修改 main.py 的 _check_dangerous()
    - 这样查磁盘就不用每次确认了

18. 把 max_turns 从 12 改成 5
    - 试试能不能完成第二关的第 6 题
    - 观察不同 max_turns 对复杂任务的影响

19. 禁用掉 python_repl 工具
    - 然后在学习模式里说 "帮我算 1+1"
    - 看看 Agent 会怎么做（可能会用 bash 的 python -c）
```

---

## 💡 第六关：面试模拟（口头回答）

```
20. Agent Loop 的核心循环有几步？画出来

21. LLM 怎么知道有哪些工具可用？具体数据格式是什么样的？

22. 工具执行失败后 Agent 会怎么做？代码在哪里实现的？

23. 为什么需要 Skill Selector？它解决了什么问题？

24. 如果想接入 Claude 而不是 DeepSeek，需要改哪些文件？
```

---

## 任务追踪表

```
第一关:  □ 1  □ 2  □ 3  □ 4
第二关:  □ 5  □ 6  □ 7
第三关:  □ 8  □ 9  □ 10
第四关:  □ 11 □ 12 □ 13 □ 14 □ 15
第五关:  □ 16 □ 17 □ 18 □ 19
第六关:  □ 20 □ 21 □ 22 □ 23 □ 24
```

按顺序来，先全部通关第一关，再往后。
